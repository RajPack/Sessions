import { printOnScreen, clearScreen, executeWhenReady } from './utils';
import { defer, from, interval, zip, of } from 'rxjs';
import { delay, map } from 'rxjs/operators';

function lesson1() {
    
}

executeWhenReady(lesson1);
